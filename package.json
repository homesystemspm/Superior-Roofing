{
  "name": "superior-roofing-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/postbuild.mjs",
    "preview": "vite preview",
    "process-images": "powershell -ExecutionPolicy Bypass -File ./scripts/process-images.ps1"
  },
  "devDependencies": {
    "vite": "^7.1.3"
  }
}
